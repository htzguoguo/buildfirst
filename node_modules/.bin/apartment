#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../apartment/bin/apartment" "$@"
  ret=$?
else 
  node  "$basedir/../apartment/bin/apartment" "$@"
  ret=$?
fi
exit $ret
